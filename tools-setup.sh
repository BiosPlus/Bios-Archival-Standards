#!usr/bin/env bash
#The tools I commonly use and a quick installer to grab them all in bulk.
#To run this execute the following: curl https://raw.githubusercontent.com/BiosPlus/Bios-Archival-Standards/master/tools-setup.sh | sudo bash

sudo apt-get install wget2 screen fuse htop python3 && sudo pip install youtube-dl
